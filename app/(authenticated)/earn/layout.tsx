/**
 * Earn layout - wraps earn pages.
 * Auth is handled by the parent (authenticated) layout.
 */
export default function EarnLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
